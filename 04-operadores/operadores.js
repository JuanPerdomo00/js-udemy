/* 
! Operadores
*/

// Operador de asignacion
var x = 3;

// Operador de asignacion de adicion (+=)
var x = 2;
var y = 1;

console.log((x += y));

// y haci mismo con el resto de opedadores (+, -. *, /, **, %)
